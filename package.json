{
  "name": "the-match-garden",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "build": "echo 'No build step required'",
    "lint": "oxlint",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "oxlint": "^1.75.0",
    "vitest": "^3.2.4"
  }
}
